{
  "$defs": {
    "AutoStopSection": {
      "additionalProperties": false,
      "description": "Metric-based automatic stopping.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable metric-based auto-stop.",
          "title": "Enabled",
          "type": "boolean"
        },
        "metrics": {
          "default": "val_loss",
          "description": "Metric monitored for auto-stop decisions.",
          "enum": [
            "loss",
            "val_loss"
          ],
          "title": "Metrics",
          "type": "string"
        },
        "patience": {
          "default": 30,
          "description": "Number of non-improving epochs tolerated before stop.",
          "minimum": 0,
          "title": "Patience",
          "type": "integer"
        }
      },
      "title": "AutoStopSection",
      "type": "object"
    },
    "ContinueTrainingExperimentSection": {
      "additionalProperties": false,
      "description": "Experiment metadata for resuming optimization inside an existing run directory.",
      "properties": {
        "mode": {
          "const": "continue_training",
          "default": "continue_training",
          "description": "Resume training in-place from an existing run. The original experiment name and run directory are reused automatically.",
          "title": "Mode",
          "type": "string"
        },
        "post_training_inference": {
          "default": true,
          "description": "Whether to trigger automatic post-training evaluation when the resumed training completes or stops early.",
          "title": "Post Training Inference",
          "type": "boolean"
        }
      },
      "title": "ContinueTrainingExperimentSection",
      "type": "object"
    },
    "ExperimentLoadModelSection": {
      "additionalProperties": true,
      "description": "User-authored settings for loading an existing model into a training run.",
      "properties": {
        "canonical_load": {
          "default": true,
          "description": "Whether the source run should be resolved through the canonical model directory routing.",
          "title": "Canonical Load",
          "type": "boolean"
        },
        "dataset_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Dataset name of the source run to load from when using canonical loading.",
          "title": "Dataset Name"
        },
        "subfolder": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional model subfolder used to locate the source run under the models root.",
          "title": "Subfolder"
        },
        "exp_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Experiment name of the source run to load from.",
          "title": "Exp Name"
        },
        "name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Legacy alias for the source experiment name.",
          "title": "Name"
        },
        "model_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Legacy alias for the model or run name to load.",
          "title": "Model Name"
        },
        "model_full_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Absolute path to a specific model directory or checkpoint when bypassing canonical routing.",
          "title": "Model Full Path"
        },
        "load_method": {
          "anyOf": [
            {
              "enum": [
                "state_dict",
                "full_model"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Checkpoint serialization format to load, typically state_dict or full_model.",
          "title": "Load Method"
        },
        "best_or_last": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Checkpoint selector used when epoch_number is not set, usually best or last.",
          "title": "Best Or Last"
        },
        "epoch_number": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Specific checkpoint epoch to load instead of using best_or_last.",
          "title": "Epoch Number"
        }
      },
      "title": "ExperimentLoadModelSection",
      "type": "object"
    },
    "HDNSafeResumeConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "auto_use_safe_checkpoint_on_continue": {
          "default": true,
          "title": "Auto Use Safe Checkpoint On Continue",
          "type": "boolean"
        },
        "drop_optimizer_scheduler_state_on_safe_resume": {
          "default": false,
          "title": "Drop Optimizer Scheduler State On Safe Resume",
          "type": "boolean"
        },
        "rewind_steps": {
          "default": 1,
          "minimum": 0,
          "title": "Rewind Steps",
          "type": "integer"
        },
        "lr_scale": {
          "default": 0.2,
          "exclusiveMinimum": 0.0,
          "title": "Lr Scale",
          "type": "number"
        },
        "min_lr": {
          "default": 1e-08,
          "exclusiveMinimum": 0.0,
          "title": "Min Lr",
          "type": "number"
        },
        "max_compound_steps": {
          "anyOf": [
            {
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Max Compound Steps"
        },
        "force_grad_clip_max_norm": {
          "anyOf": [
            {
              "exclusiveMinimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Force Grad Clip Max Norm"
        }
      },
      "title": "HDNSafeResumeConfig",
      "type": "object"
    },
    "RecoveryConfig": {
      "additionalProperties": false,
      "properties": {
        "hdn_safe_resume": {
          "$ref": "#/$defs/HDNSafeResumeConfig"
        }
      },
      "title": "RecoveryConfig",
      "type": "object"
    },
    "SavingSection": {
      "additionalProperties": true,
      "description": "Checkpoint and validation-image saving behavior.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Whether checkpoints and other training artifacts should be written to disk.",
          "title": "Enabled",
          "type": "boolean"
        },
        "canonical_save": {
          "default": true,
          "description": "Whether checkpoint/output paths should be resolved through the canonical project routing rules.",
          "title": "Canonical Save",
          "type": "boolean"
        },
        "validation_images": {
          "default": true,
          "description": "Whether to save validation image previews during training.",
          "title": "Validation Images",
          "type": "boolean"
        },
        "validation_freq": {
          "default": 10,
          "description": "Number of epochs between validation-image saves.",
          "title": "Validation Freq",
          "type": "integer"
        },
        "state_dict": {
          "default": false,
          "description": "Whether checkpoints should be saved as state_dict files.",
          "title": "State Dict",
          "type": "boolean"
        },
        "entire_model": {
          "default": false,
          "description": "Whether checkpoints should be saved as serialized full-model files.",
          "title": "Entire Model",
          "type": "boolean"
        },
        "overwrite_best": {
          "default": true,
          "description": "Whether the current best checkpoint may overwrite the previously saved best checkpoint.",
          "title": "Overwrite Best",
          "type": "boolean"
        }
      },
      "title": "SavingSection",
      "type": "object"
    },
    "TensorboardSection": {
      "additionalProperties": true,
      "description": "TensorBoard logging settings.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether TensorBoard summaries should be written during training.",
          "title": "Enabled",
          "type": "boolean"
        }
      },
      "title": "TensorboardSection",
      "type": "object"
    },
    "TrainingSection": {
      "additionalProperties": true,
      "description": "Core optimization settings for the training loop.",
      "properties": {
        "n_epochs": {
          "default": 1,
          "description": "Maximum number of training epochs to run.",
          "title": "N Epochs",
          "type": "integer"
        },
        "batch_size": {
          "default": 1,
          "description": "Fallback batch size used by the trainer when the data section does not provide one.",
          "title": "Batch Size",
          "type": "integer"
        },
        "learning_rate": {
          "default": 0.0001,
          "description": "Initial learning rate passed to the optimizer.",
          "title": "Learning Rate",
          "type": "number"
        },
        "optimizer": {
          "default": "Adam",
          "description": "Optimizer name used to update the model parameters.",
          "title": "Optimizer",
          "type": "string"
        },
        "scheduler": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional learning-rate scheduler. Use a string name (legacy) or an object with `name` plus scheduler kwargs.",
          "title": "Scheduler"
        },
        "progress_bar": {
          "default": false,
          "description": "Whether to show a live progress bar during training iterations.",
          "title": "Progress Bar",
          "type": "boolean"
        },
        "early_stop": {
          "default": false,
          "description": "Legacy debug-stop flag. Keeps historical truncated-training behavior for backward compatibility.",
          "title": "Early Stop",
          "type": "boolean"
        },
        "debug_stop": {
          "default": false,
          "description": "Debug helper: when true, stop after 3 full epochs.",
          "title": "Debug Stop",
          "type": "boolean"
        },
        "pos_encod": {
          "default": false,
          "description": "Whether positional encoding should be enabled for models that support it.",
          "title": "Pos Encod",
          "type": "boolean"
        },
        "max_grad_norm": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "To clip grad norm value. Set to None to disable gradient clipping.",
          "title": "Max Grad Norm"
        },
        "val_loss_patience": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "deprecated": true,
          "description": "Deprecated and ignored. Use `training.auto_stop.patience` instead.",
          "title": "Val Loss Patience"
        },
        "warmup": {
          "$ref": "#/$defs/WarmupSection",
          "description": "Optional optimizer-step warmup. Active only when scheduler is ReduceLROnPlateau."
        },
        "auto_stop": {
          "$ref": "#/$defs/AutoStopSection",
          "description": "Optional metric-based automatic stopping."
        }
      },
      "title": "TrainingSection",
      "type": "object"
    },
    "WarmupSection": {
      "additionalProperties": false,
      "description": "Manual optimizer-step warmup settings.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable optimizer-step LR warmup.",
          "title": "Enabled",
          "type": "boolean"
        },
        "steps": {
          "default": 500,
          "description": "Number of optimizer steps used for warmup.",
          "minimum": 0,
          "title": "Steps",
          "type": "integer"
        },
        "start_factor": {
          "default": 0.1,
          "description": "Initial LR factor relative to base LR at warmup start.",
          "exclusiveMinimum": 0.0,
          "maximum": 1.0,
          "title": "Start Factor",
          "type": "number"
        }
      },
      "title": "WarmupSection",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Sparse authoring schema for resuming a previous run in-place.",
  "properties": {
    "experiment": {
      "$ref": "#/$defs/ContinueTrainingExperimentSection",
      "description": "Mode-specific metadata for resuming training in the original run directory."
    },
    "training": {
      "$ref": "#/$defs/TrainingSection",
      "description": "Optional optimization overrides to apply while continuing the original run."
    },
    "saving": {
      "$ref": "#/$defs/SavingSection",
      "description": "Optional checkpoint and validation-image saving overrides for the resumed run."
    },
    "tensorboard": {
      "$ref": "#/$defs/TensorboardSection",
      "description": "Optional TensorBoard logging overrides for the resumed run."
    },
    "load_model": {
      "$ref": "#/$defs/ExperimentLoadModelSection",
      "description": "Required source-run selector identifying which existing run should be resumed."
    },
    "recovery": {
      "$ref": "#/$defs/RecoveryConfig",
      "description": "Recovery behavior for safe resume on continue_training."
    },
    "metadata": {
      "type": "object",
      "title": "ConfigMetadata",
      "description": "Catalog metadata used by `lisai configs`; stripped before runtime validation.",
      "additionalProperties": true,
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "preset",
            "template",
            "example",
            "local"
          ],
          "description": "Config catalog kind."
        },
        "name": {
          "type": "string",
          "description": "Human-readable config name."
        },
        "task": {
          "type": "string",
          "description": "Associated training task name."
        },
        "description": {
          "type": "string",
          "description": "Short catalog description."
        }
      }
    }
  },
  "required": [
    "load_model"
  ],
  "title": "ContinueTrainingConfig",
  "type": "object"
}
