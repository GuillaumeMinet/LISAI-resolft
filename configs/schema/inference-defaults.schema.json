{
  "$defs": {
    "ApplyDefaults": {
      "additionalProperties": false,
      "description": "Fully resolved defaults for the `apply` inference section.",
      "properties": {
        "save_folder": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": "default",
          "description": "Output folder for saved predictions or evaluation artifacts. Use 'default' for apply to save next to the inputs, or null for evaluate to save inside the model run directory.",
          "title": "Save Folder"
        },
        "in_place": {
          "default": false,
          "description": "Whether apply outputs should be written back into the input location.",
          "title": "In Place",
          "type": "boolean"
        },
        "epoch_number": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Explicit checkpoint epoch number to load. Use null to select the checkpoint through best_or_last instead.",
          "title": "Epoch Number"
        },
        "best_or_last": {
          "default": "best",
          "description": "Checkpoint selector used when epoch_number is null. Supported values are 'best', 'last', and 'both'.",
          "enum": [
            "best",
            "last",
            "both"
          ],
          "title": "Best Or Last",
          "type": "string"
        },
        "filters": {
          "description": "File extensions accepted when apply input points to a directory.",
          "items": {
            "type": "string"
          },
          "title": "Filters",
          "type": "array"
        },
        "skip_if_contain": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional substrings; matching filenames are skipped during apply.",
          "title": "Skip If Contain"
        },
        "crop_size": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "maxItems": 2,
              "minItems": 2,
              "prefixItems": [
                {
                  "type": "integer"
                },
                {
                  "type": "integer"
                }
              ],
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional center crop used before inference. Use a single integer for a square crop or a height,width tuple.",
          "title": "Crop Size"
        },
        "keep_original_shape": {
          "default": true,
          "description": "Whether outputs should be padded back to the original spatial size after cropped inference.",
          "title": "Keep Original Shape",
          "type": "boolean"
        },
        "tiling_size": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "enum": [
                "auto",
                "off"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": "auto",
          "description": "Tile size used for patch-wise inference. Use 'auto' or null to fall back to the saved model default, use a positive integer to force a tile size, or use 'off' to disable tiling.",
          "title": "Tiling Size"
        },
        "stack_selection_idx": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional stack or channel index selected before converting the input to a 4D tensor.",
          "title": "Stack Selection Idx"
        },
        "timelapse_max": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional maximum number of timelapse frames to process during apply.",
          "title": "Timelapse Max"
        },
        "lvae_num_samples": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 20,
          "description": "Number of stochastic samples drawn when running LVAE models.",
          "title": "Lvae Num Samples"
        },
        "lvae_save_samples": {
          "default": true,
          "description": "Whether individual LVAE samples should be saved in addition to the main prediction.",
          "title": "Lvae Save Samples",
          "type": "boolean"
        },
        "denormalize_output": {
          "default": true,
          "description": "Whether model outputs should be converted back from normalized model space before saving.",
          "title": "Denormalize Output",
          "type": "boolean"
        },
        "save_inp": {
          "default": false,
          "description": "Whether the input image or tensor should also be saved alongside predictions.",
          "title": "Save Inp",
          "type": "boolean"
        },
        "downsamp": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional spatial downsampling factor applied to the apply input before inference.",
          "title": "Downsamp"
        },
        "fill_factor": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "maximum": 1,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional fill factor used with deterministic 'multiple' apply downsampling. When set, apply uses generate_downsamp_inp with random=false.",
          "title": "Fill Factor"
        },
        "apply_color_code": {
          "default": false,
          "description": "Whether an additional color-coded visualization should be saved for volumetric apply outputs.",
          "title": "Apply Color Code",
          "type": "boolean"
        },
        "color_code_prm": {
          "$ref": "#/$defs/ColorCodeDefaults",
          "description": "Nested volumetric color-coding settings used when apply_color_code is true."
        },
        "dark_frame_context_length": {
          "default": false,
          "description": "Whether missing timelapse context frames should be replaced with dark frames instead of skipping sequence edges.",
          "title": "Dark Frame Context Length",
          "type": "boolean"
        }
      },
      "title": "ApplyDefaults",
      "type": "object"
    },
    "ColorCodeDefaults": {
      "additionalProperties": false,
      "description": "Complete color-coding settings used for volumetric apply outputs.",
      "properties": {
        "colormap": {
          "default": "turbo",
          "description": "Matplotlib colormap name used for volumetric color-coding.",
          "title": "Colormap",
          "type": "string"
        },
        "saturation": {
          "default": 0.35,
          "description": "Contrast boost applied to the color-coded volumetric visualization.",
          "title": "Saturation",
          "type": "number"
        },
        "add_colorbar": {
          "default": true,
          "description": "Whether a colorbar should be appended to the color-coded volumetric visualization.",
          "title": "Add Colorbar",
          "type": "boolean"
        },
        "zstep": {
          "default": 0.4,
          "description": "Physical or logical spacing between z slices used when scaling the volumetric colorbar.",
          "title": "Zstep",
          "type": "number"
        }
      },
      "title": "ColorCodeDefaults",
      "type": "object"
    },
    "EvaluateDefaults": {
      "additionalProperties": false,
      "description": "Fully resolved defaults for the `evaluate` inference section.",
      "properties": {
        "best_or_last": {
          "default": "best",
          "description": "Checkpoint selector used when epoch_number is null. Supported values are 'best', 'last', and 'both'.",
          "enum": [
            "best",
            "last",
            "both"
          ],
          "title": "Best Or Last",
          "type": "string"
        },
        "epoch_number": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Explicit checkpoint epoch number to load. Use null to select the checkpoint through best_or_last instead.",
          "title": "Epoch Number"
        },
        "tiling_size": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "enum": [
                "auto",
                "off"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": "auto",
          "description": "Tile size used for patch-wise inference. Use 'auto' or null to fall back to the saved model default, use a positive integer to force a tile size, or use 'off' to disable tiling.",
          "title": "Tiling Size"
        },
        "crop_size": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "maxItems": 2,
              "minItems": 2,
              "prefixItems": [
                {
                  "type": "integer"
                },
                {
                  "type": "integer"
                }
              ],
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional center crop used before inference. Use a single integer for a square crop or a height,width tuple.",
          "title": "Crop Size"
        },
        "metrics_list": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Metrics to compute during evaluation, for example ['psnr', 'ssim']. Use null to skip metrics.",
          "title": "Metrics List"
        },
        "lvae_num_samples": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 20,
          "description": "Number of stochastic samples drawn when running LVAE models.",
          "title": "Lvae Num Samples"
        },
        "results": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional in-memory metrics accumulator used by the Python API when evaluating programmatically.",
          "title": "Results"
        },
        "save_folder": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Output folder for saved predictions or evaluation artifacts. Use 'default' for apply to save next to the inputs, or null for evaluate to save inside the model run directory.",
          "title": "Save Folder"
        },
        "overwrite": {
          "default": false,
          "description": "Whether an existing save folder may be overwritten.",
          "title": "Overwrite",
          "type": "boolean"
        },
        "eval_gt": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional evaluation ground-truth path/key. If omitted, evaluation uses the dataset registry eval_gt default when present, otherwise the training target. Use @training to force the saved training target and @none to evaluate without ground truth.",
          "title": "Eval Gt"
        },
        "data_prm_update": {
          "anyOf": [
            {
              "additionalProperties": true,
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional extra data-loader overrides such as {'data_dir': '...'} or {'subfolder': '...'} used during evaluation.",
          "title": "Data Prm Update"
        },
        "ch_out": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 1,
          "description": "Output channel count forwarded to inference. Defaults to 1 for evaluation because models are trained to predict a single target frame even when inputs contain multiple context channels.",
          "title": "Ch Out"
        },
        "split": {
          "default": "test",
          "description": "Dataset split to evaluate, typically 'test' or 'val'.",
          "title": "Split",
          "type": "string"
        },
        "limit_n_imgs": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional cap on the number of images or batches evaluated.",
          "title": "Limit N Imgs"
        }
      },
      "title": "EvaluateDefaults",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Fully resolved inference settings used at runtime.\n\nThis object represents the complete inference configuration after defaults\nhave been applied. It is the typed counterpart of the final merged settings\nconsumed by apply/evaluate entrypoints.",
  "properties": {
    "apply": {
      "$ref": "#/$defs/ApplyDefaults",
      "description": "Fully resolved settings used by the `lisai apply` flow."
    },
    "evaluate": {
      "$ref": "#/$defs/EvaluateDefaults",
      "description": "Fully resolved settings used by the `lisai evaluate` flow."
    }
  },
  "title": "ResolvedInferenceConfig",
  "type": "object"
}
