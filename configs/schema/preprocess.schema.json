{
  "title": "PreprocessConfig",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "dataset_name": {
      "description": "Dataset name as registered in the project data paths and registry.",
      "title": "Dataset Name",
      "type": "string"
    },
    "pipeline": {
      "description": "Preprocess pipeline to run. This selects the valid schema for pipeline_cfg.",
      "title": "Pipeline",
      "type": "string",
      "enum": [
        "paired_single_recon",
        "recon_mltpl_snr",
        "recon_timelapse_simple",
        "single_recon"
      ]
    },
    "data_type": {
      "description": "Dataset data type produced by preprocessing, typically raw or recon.",
      "title": "Data Type",
      "type": "string",
      "enum": [
        "raw",
        "recon"
      ]
    },
    "fmt": {
      "description": "Output data format used for canonical naming and saving templates.",
      "title": "Fmt",
      "type": "string",
      "enum": [
        "mltpl_snr",
        "single",
        "timelapse"
      ]
    },
    "usage": {
      "default": "training",
      "description": "Intended dataset usage in the registry: training datasets may also be evaluated, evaluation datasets are whole-dataset evaluation-only resources.",
      "enum": [
        "training",
        "evaluation"
      ],
      "title": "Usage",
      "type": "string"
    },
    "pipeline_cfg": {
      "additionalProperties": true,
      "description": "Pipeline-specific configuration. Editor hints depend on the selected pipeline.",
      "title": "Pipeline Cfg",
      "type": "object"
    },
    "log": {
      "$ref": "#/$defs/PreprocessLogConfig",
      "description": "Options controlling YAML manifest logging for the preprocess run."
    },
    "registry": {
      "$ref": "#/$defs/PreprocessRegistryConfig",
      "description": "Options controlling metadata written to the dataset registry."
    },
    "split": {
      "$ref": "#/$defs/PreprocessSplitConfig",
      "description": "Options controlling optional train, validation, and test split assignment for training datasets."
    }
  },
  "required": [
    "dataset_name",
    "pipeline",
    "data_type",
    "fmt"
  ],
  "allOf": [
    {
      "if": {
        "properties": {
          "pipeline": {
            "const": "paired_single_recon"
          }
        },
        "required": [
          "pipeline"
        ]
      },
      "then": {
        "properties": {
          "data_type": {
            "description": "Dataset data type produced by preprocessing, typically raw or recon. Allowed for pipeline 'paired_single_recon': recon.",
            "title": "Data Type",
            "type": "string",
            "const": "recon",
            "default": "recon"
          },
          "fmt": {
            "description": "Output data format used for canonical naming and saving templates. Allowed for pipeline 'paired_single_recon': single.",
            "title": "Fmt",
            "type": "string",
            "const": "single",
            "default": "single"
          },
          "pipeline_cfg": {
            "description": "Preprocess paired 2D reconstructed images stored in two folders.",
            "properties": {
              "base_subfolder": {
                "default": "",
                "description": "Optional common parent folder inside dataset dump/recon. Input, GT, and auxiliary folders are resolved beneath it.",
                "title": "Base Subfolder",
                "type": "string"
              },
              "dump_subfolder": {
                "default": "",
                "description": "Deprecated alias for base_subfolder. Kept for backward compatibility with existing paired_single_recon configs.",
                "title": "Dump Subfolder",
                "type": "string"
              },
              "input_subfolder": {
                "default": "low",
                "description": "Folder containing the model input images, relative to base_subfolder. Its name is preserved as the processed input folder.",
                "title": "Input Subfolder",
                "type": "string"
              },
              "gt_subfolder": {
                "default": "high",
                "description": "Folder containing the paired evaluation ground-truth images, relative to base_subfolder. Its name is preserved as the processed GT output folder.",
                "title": "Gt Subfolder",
                "type": "string"
              },
              "registration": {
                "default": false,
                "description": "If true, register each input image to its paired ground-truth image before saving.",
                "title": "Registration",
                "type": "boolean"
              },
              "crop_size": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Optional centered crop size applied identically to each paired input and ground-truth image.",
                "title": "Crop Size"
              },
              "auxiliary_subfolders": {
                "additionalProperties": {
                  "$ref": "#/$defs/AuxiliarySubfolderConfig"
                },
                "description": "Optional named auxiliary image folders relative to base_subfolder. Each auxiliary is saved as role 'aux' and must explicitly configure matching. Files can be matched by exact filename or by acquisition timestamp.",
                "title": "Auxiliary Subfolders",
                "type": "object"
              }
            },
            "title": "PairedSingleReconConfig",
            "type": "object",
            "additionalProperties": false
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "pipeline": {
            "const": "recon_mltpl_snr"
          }
        },
        "required": [
          "pipeline"
        ]
      },
      "then": {
        "properties": {
          "data_type": {
            "description": "Dataset data type produced by preprocessing, typically raw or recon. Allowed for pipeline 'recon_mltpl_snr': recon.",
            "title": "Data Type",
            "type": "string",
            "const": "recon",
            "default": "recon"
          },
          "fmt": {
            "description": "Output data format used for canonical naming and saving templates. Allowed for pipeline 'recon_mltpl_snr': mltpl_snr.",
            "title": "Fmt",
            "type": "string",
            "const": "mltpl_snr",
            "default": "mltpl_snr"
          },
          "pipeline_cfg": {
            "description": "Preprocess reconstructed multiple-SNR stacks (T,Y,X).\n\nRefactored from legacy `recon_mltpl_snr` pipeline.\n\nTypical stack layouts:\n  - first_low_inp=False:\n        [snr0, snr1, snr2, ...]  -> inp_mltpl_snr is the full stack\n  - first_low_inp=True:\n        [low_inp, snr0, snr1, ...] -> inp_single is first frame, inp_mltpl_snr is remaining stack\n\nOutputs (depending on config):\n  - inp_mltpl_snr (TYX)\n  - inp_single (YX) (optional)\n  - gt_snr0 (YX) (optional)\n  - gt_avg (YX) (optional)\n\nNotes:\n  - Registration uses pystackreg via `register_stack` (rigid-body) if enabled.\n  - `gt_avg_n_frames` controls how many leading frames are averaged for gt_avg.",
            "properties": {
              "base_subfolder": {
                "default": "",
                "description": "Optional common parent folder inside dataset dump/recon.",
                "title": "Base Subfolder",
                "type": "string"
              },
              "input_subfolder": {
                "default": "",
                "description": "Optional folder containing the primary multiple-SNR stack files, relative to base_subfolder.",
                "title": "Input Subfolder",
                "type": "string"
              },
              "dump_subfolder": {
                "default": "",
                "description": "Deprecated alias for base_subfolder. Kept for backward compatibility with existing recon_mltpl_snr configs.",
                "title": "Dump Subfolder",
                "type": "string"
              },
              "combine_subfolders": {
                "default": false,
                "description": "If true, scan the immediate subfolders of the dump root and combine their files into one preprocess stream.",
                "title": "Combine Subfolders",
                "type": "boolean"
              },
              "first_low_inp": {
                "default": false,
                "description": "If true, treat the first frame as a dedicated low-SNR single input and save the remaining frames as the multi-SNR input stack.",
                "title": "First Low Inp",
                "type": "boolean"
              },
              "registration": {
                "default": true,
                "description": "If true, rigidly register the stack before generating outputs.",
                "title": "Registration",
                "type": "boolean"
              },
              "crop_size": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Optional centered crop size applied to the stack before outputs are extracted.",
                "title": "Crop Size"
              },
              "gt_types": {
                "anyOf": [
                  {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Optional list of ground-truth outputs to create. Allowed values are 'snr0' and 'avg'.",
                "title": "Gt Types"
              },
              "gt_clip_neg": {
                "default": false,
                "description": "If true, clamp negative values to zero in generated ground-truth outputs.",
                "title": "Gt Clip Neg",
                "type": "boolean"
              },
              "gt_avg_n_frames": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Optional number of leading frames to average when creating the gt_avg output. If omitted, all available GT frames are averaged.",
                "title": "Gt Avg N Frames"
              }
            },
            "title": "ReconMltplSnrConfig",
            "type": "object",
            "additionalProperties": false
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "pipeline": {
            "const": "recon_timelapse_simple"
          }
        },
        "required": [
          "pipeline"
        ]
      },
      "then": {
        "properties": {
          "data_type": {
            "description": "Dataset data type produced by preprocessing, typically raw or recon. Allowed for pipeline 'recon_timelapse_simple': recon.",
            "title": "Data Type",
            "type": "string",
            "const": "recon",
            "default": "recon"
          },
          "fmt": {
            "description": "Output data format used for canonical naming and saving templates. Allowed for pipeline 'recon_timelapse_simple': timelapse.",
            "title": "Fmt",
            "type": "string",
            "const": "timelapse",
            "default": "timelapse"
          },
          "pipeline_cfg": {
            "description": "Preprocess reconstructed timelapse stacks (T,Y,X) from dump/ into preprocess/recon/.\n\nThis pipeline is the refactored version of the legacy `recon_timelapse_upsamp` pipeline.\n\nOperations (all optional):\n  - remove_first_frame\n  - bleach_correction (simple ratio)\n  - center crop\n  - clip negatives to 0",
            "properties": {
              "base_subfolder": {
                "default": "",
                "description": "Optional common parent folder inside dataset dump/recon.",
                "title": "Base Subfolder",
                "type": "string"
              },
              "input_subfolder": {
                "default": "",
                "description": "Optional folder containing the primary timelapse files, relative to base_subfolder.",
                "title": "Input Subfolder",
                "type": "string"
              },
              "dump_subfolder": {
                "default": "",
                "description": "Deprecated alias for base_subfolder. Kept for backward compatibility with existing recon_timelapse_simple configs.",
                "title": "Dump Subfolder",
                "type": "string"
              },
              "combine_subfolders": {
                "default": false,
                "description": "If true, scan the immediate subfolders of the dump root and combine their files into one preprocess stream.",
                "title": "Combine Subfolders",
                "type": "boolean"
              },
              "crop_size": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Optional centered crop size applied to each timelapse frame before saving.",
                "title": "Crop Size"
              },
              "clip_neg": {
                "default": false,
                "description": "If true, clamp negative pixel values to zero after preprocessing.",
                "title": "Clip Neg",
                "type": "boolean"
              },
              "remove_first": {
                "default": false,
                "description": "If true, drop the first frame of each timelapse before any other processing.",
                "title": "Remove First",
                "type": "boolean"
              },
              "bleach_correction": {
                "default": false,
                "description": "If true, apply simple-ratio bleach correction to each timelapse stack.",
                "title": "Bleach Correction",
                "type": "boolean"
              }
            },
            "title": "ReconTimelapseSimpleConfig",
            "type": "object",
            "additionalProperties": false
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "pipeline": {
            "const": "single_recon"
          }
        },
        "required": [
          "pipeline"
        ]
      },
      "then": {
        "properties": {
          "data_type": {
            "description": "Dataset data type produced by preprocessing, typically raw or recon. Allowed for pipeline 'single_recon': recon.",
            "title": "Data Type",
            "type": "string",
            "const": "recon",
            "default": "recon"
          },
          "fmt": {
            "description": "Output data format used for canonical naming and saving templates. Allowed for pipeline 'single_recon': single.",
            "title": "Fmt",
            "type": "string",
            "const": "single",
            "default": "single"
          },
          "pipeline_cfg": {
            "description": "Preprocess reconstructed single images from dump/ into preprocess/recon/.",
            "properties": {
              "base_subfolder": {
                "default": "",
                "description": "Optional common parent folder inside dataset dump/recon. Role-specific input and auxiliary folders are resolved beneath it.",
                "title": "Base Subfolder",
                "type": "string"
              },
              "input_subfolder": {
                "default": "",
                "description": "Optional folder containing the primary input images, relative to base_subfolder. For new configs, this name is also used as the processed output folder unless output_subfolder overrides it.",
                "title": "Input Subfolder",
                "type": "string"
              },
              "dump_subfolder": {
                "default": "",
                "description": "Deprecated alias for input_subfolder. Kept for backward compatibility with existing single_recon configs.",
                "title": "Dump Subfolder",
                "type": "string"
              },
              "output_subfolder": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Processed-data destination for the primary input stream. If omitted, new input_subfolder configs mirror that stream name. Set explicitly to null to save at preprocess/<data_type>/ root, or provide another relative subfolder to override the mirrored name.",
                "title": "Output Subfolder"
              },
              "combine_subfolders": {
                "default": false,
                "description": "If true, scan the immediate subfolders of the dump root and combine their files into one preprocess stream.",
                "title": "Combine Subfolders",
                "type": "boolean"
              },
              "crop_size": {
                "anyOf": [
                  {
                    "type": "integer"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Optional centered crop size applied to each 2D image before saving.",
                "title": "Crop Size"
              },
              "auxiliary_subfolders": {
                "additionalProperties": {
                  "$ref": "#/$defs/AuxiliarySubfolderConfig"
                },
                "description": "Optional named auxiliary image folders relative to base_subfolder. Each auxiliary is saved as role 'aux' and must explicitly configure matching. Files can be matched by exact filename or by acquisition timestamp.",
                "title": "Auxiliary Subfolders",
                "type": "object"
              }
            },
            "title": "SingleReconConfig",
            "type": "object",
            "additionalProperties": false
          }
        }
      }
    }
  ],
  "$defs": {
    "PreprocessLogConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "default": true,
          "description": "Whether to write a YAML preprocess manifest for this run.",
          "title": "Enabled",
          "type": "boolean"
        }
      },
      "title": "PreprocessLogConfig",
      "type": "object"
    },
    "PreprocessRegistryConfig": {
      "additionalProperties": false,
      "properties": {
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional short human-readable dataset description. It initializes the registry description but does not overwrite an existing one.",
          "title": "Description"
        },
        "defaults": {
          "$ref": "#/$defs/PreprocessRegistryDefaultsConfig",
          "description": "Optional registry default overrides merged over defaults inferred from produced outputs."
        }
      },
      "title": "PreprocessRegistryConfig",
      "type": "object"
    },
    "PreprocessRegistryDefaultsConfig": {
      "additionalProperties": false,
      "properties": {
        "input": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional registry default input path/key override for this preprocess data type.",
          "title": "Input"
        },
        "target": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional registry default supervised target path/key override. Use null when no default should be selected.",
          "title": "Target"
        },
        "eval_gt": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional registry default ground-truth path/key used for evaluation. Use null when no default should be selected.",
          "title": "Eval Gt"
        }
      },
      "title": "PreprocessRegistryDefaultsConfig",
      "type": "object"
    },
    "PreprocessSplitConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether to assign saved preprocess outputs to train, validation, and test folders.",
          "title": "Enabled",
          "type": "boolean"
        },
        "mode": {
          "default": "random",
          "description": "Split strategy to use when split.enabled is true: random, manual, or reuse.",
          "enum": [
            "random",
            "manual",
            "reuse"
          ],
          "title": "Mode",
          "type": "string"
        },
        "random": {
          "$ref": "#/$defs/PreprocessSplitRandomConfig",
          "description": "Parameters for random split assignment."
        },
        "manual": {
          "$ref": "#/$defs/PreprocessSplitManualConfig",
          "description": "Parameters for manually selecting validation and test items."
        },
        "reuse": {
          "anyOf": [
            {
              "$ref": "#/$defs/PreprocessSplitReuseConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Parameters for reusing split assignments from a previous preprocess manifest."
        }
      },
      "title": "PreprocessSplitConfig",
      "type": "object"
    },
    "PreprocessSplitManualConfig": {
      "additionalProperties": false,
      "properties": {
        "match_by": {
          "default": "source_name",
          "description": "Identifier style used to match manual split entries against source items. Use source_name for incoming filenames, source_relpath for paths relative to the dump root, or sample_id for generated preprocess names.",
          "enum": [
            "source_name",
            "source_relpath",
            "sample_id"
          ],
          "title": "Match By",
          "type": "string"
        },
        "val": {
          "description": "Items that should be assigned to the validation split.",
          "items": {
            "type": "string"
          },
          "title": "Val",
          "type": "array"
        },
        "test": {
          "description": "Items that should be assigned to the test split.",
          "items": {
            "type": "string"
          },
          "title": "Test",
          "type": "array"
        }
      },
      "title": "PreprocessSplitManualConfig",
      "type": "object"
    },
    "PreprocessSplitRandomConfig": {
      "additionalProperties": false,
      "properties": {
        "seed": {
          "default": 0,
          "description": "Random seed used to shuffle items before assigning validation and test splits.",
          "title": "Seed",
          "type": "integer"
        },
        "val_fraction": {
          "default": 0.1,
          "description": "Fraction of source items assigned to the validation split.",
          "title": "Val Fraction",
          "type": "number"
        },
        "test_fraction": {
          "default": 0.1,
          "description": "Fraction of source items assigned to the test split.",
          "title": "Test Fraction",
          "type": "number"
        }
      },
      "title": "PreprocessSplitRandomConfig",
      "type": "object"
    },
    "PreprocessSplitReuseConfig": {
      "additionalProperties": false,
      "properties": {
        "manifest_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional path to a previous preprocess YAML manifest whose split assignments should be reused.",
          "title": "Manifest Path"
        },
        "dataset_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Dataset name of a previous preprocess run to reuse split assignments from.",
          "title": "Dataset Name"
        },
        "data_type": {
          "anyOf": [
            {
              "enum": [
                "raw",
                "recon"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Data type of the previous preprocess run when reusing a split by dataset name.",
          "title": "Data Type"
        },
        "match_by": {
          "default": "source_relpath",
          "description": "Identifier style used to match current source items to entries stored in the reused manifest.",
          "enum": [
            "source_name",
            "source_relpath",
            "sample_id"
          ],
          "title": "Match By",
          "type": "string"
        }
      },
      "title": "PreprocessSplitReuseConfig",
      "type": "object"
    },
    "AuxiliarySubfolderConfig": {
      "description": "Configuration for one named auxiliary image folder.",
      "properties": {
        "matching": {
          "description": "Whether every primary sample requires an auxiliary match ('required') or unmatched primary samples are kept without that auxiliary ('optional').",
          "enum": [
            "required",
            "optional"
          ],
          "title": "Matching",
          "type": "string"
        },
        "match_by": {
          "default": "filename",
          "description": "How auxiliary files are matched to primary samples. Exact filename matching is the default; timestamp matching uses an HHhMMmSSs timestamp in filenames.",
          "enum": [
            "filename",
            "timestamp"
          ],
          "title": "Match By",
          "type": "string"
        },
        "max_time_delta_s": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Maximum allowed timestamp difference in seconds. Required when match_by is 'timestamp'.",
          "title": "Max Time Delta S"
        },
        "timestamp_relation": {
          "default": "either",
          "description": "For timestamp matching, restrict auxiliary acquisitions to before the primary, after the primary, or either side.",
          "enum": [
            "before_primary",
            "after_primary",
            "either"
          ],
          "title": "Timestamp Relation",
          "type": "string"
        }
      },
      "required": [
        "matching"
      ],
      "title": "AuxiliarySubfolderConfig",
      "type": "object"
    }
  }
}
